---
- hosts: localhost
  gather_facts: no
  tasks:
    - name: Execute clone command
      command: echo "hello world"
      register: result

    - name: Show command result
      debug:
        var: result.stdout
